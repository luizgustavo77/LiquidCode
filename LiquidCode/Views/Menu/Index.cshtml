@model List<CoffeeDto>

@{
    ViewData["Title"] = "Cardápio";
}

<script src="~/js/adicional/cardapio.js" asp-append-version="true"></script>
<script>

    var storageList = @Html.Raw(Json.Serialize(Model));

        $(document).ready(function () {
            mostrarLoading();
            cardapio().montarTabela();
        });
</script>

<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
    <div class="table-responsive">
        <table id="tabela" class="table table-hover table-striped tabela-100-por-cento"></table>
    </div>
</div>
